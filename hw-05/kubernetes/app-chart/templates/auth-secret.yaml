apiVersion: v1
kind: Secret
metadata:
  name: auth-secret
  namespace: app
type: Opaque
stringData:
  APP_SECRET_DATASOURCE_USERNAME: {{ .Values.auth.postgres.username }}
  APP_SECRET_DATASOURCE_PASSWORD: {{ .Values.auth.postgres.password }}
