postgres:
  host: postgres-postgresql.app.svc.cluster.local
  port: 5432
  database: hw
  #it is fine to add this password to a version control system because it is an educational project
  password: postgres123
  username: postgres

auth:
  postgres:
    #it is fine to add this password to a version control system because it is an educational project
    password: auth123
    username: auth

profile:
  postgres:
    #it is fine to add this password to a version control system because it is an educational project
    password: profile123
    username: profile
